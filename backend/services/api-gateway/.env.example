PORT=3000

# Service URLs
# Desde Docker: usar nombres de servicio (auth-service, operations-service)
# Desde local: usar localhost:PORT
AUTH_URL=http://auth-service:3001
AUTH_SERVICE_URL=http://auth-service:3001
OPERATIONS_SERVICE_URL=http://operations-service:3002
OPS_URL=http://operations-service:3002
TRACK_URL=ws://tracking-service:3003
TRACKING_SERVICE_URL=http://tracking-service:3003

# JWT (debe coincidir con auth-service)
JWT_ACCESS_SECRET=super_access_secret

# Rutas públicas (separadas por coma, sin espacios extras)
JWT_PUBLIC_ROUTES=/auth/signup,/auth/signin,/auth/google,/auth/google/callback

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:3000

# Nota WebSocket:
# Para tracking WebSocket (ws://tracking-service:3003/ws/track),
# se recomienda que el frontend se conecte directamente al tracking-service
# con CORS/allowed origins ya configurados.
# El gateway NO hace proxy de WebSocket.

# Policy Hints (opcional - validación temprana de roles)
# Si está en 'true', el gateway puede negar acceso temprano basado en roles
# Ejemplo: /ops/admin/* requiere rol ADMIN
JWT_POLICY_HINTS=false

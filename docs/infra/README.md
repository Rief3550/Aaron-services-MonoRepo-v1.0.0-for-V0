# Nginx Configuration (Opcional)

Este directorio está reservado para configuraciones de Nginx cuando se implemente TLS/HTTP2.

## Uso

Si deseas usar Nginx como reverse proxy:

1. Crear archivo `nginx.conf`
2. Configurar SSL/TLS
3. Actualizar `docker-compose.yml` para incluir el servicio Nginx

